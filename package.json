{
  "name": "todo-stickies",
  "private": true,
  "main": "electron/main.js",
  "scripts": {
    "dev": "concurrently \"npm:start --prefix electron\" \"npm:dev --prefix renderer\"",
    "start": "electron electron/main.js",
    "build": "npm run build --prefix renderer"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "wait-on": "^7.0.0"
  },
  "dependencies": {
    "electron": "^31.0.0",
    "react-icons": "^5.5.0"
  }
}
